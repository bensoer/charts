apiVersion: v1
kind: Service
metadata:
  name: {{ include "price-buddy.fullname" . }}
  labels:
    {{- include "price-buddy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "price-buddy.selectorLabels" . | nindent 4 }}
